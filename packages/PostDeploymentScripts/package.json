{
  "name": "postdeploymentscripts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf dist && npx tsc && node ./dist/index.js",
    "build": "rm -rf dist && npx tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.6.0",
    "@typescript-eslint/parser": "^5.6.0",
    "eslint": "^8.4.1"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.43.0",
    "@aws-sdk/client-sagemaker": "^3.44.0"
  }
}
